@model ChordsTransposer.Models.ChordList

@{
    ViewBag.Title = "Test";
}

<h2>Test</h2>

@using (Ajax.BeginForm("TestResult","Test",
                            new AjaxOptions { UpdateTargetId = "divTest" }))  
{
<div id="divTest">
    <div class="editor-label">
    @Html.LabelFor(model=>model.userChords)
        </div>
    <div class="editor-field">
    @Html.EditorFor (model=>model.userChords)
        </div>
    <button>Transpose</button>
</div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")  
}

